# Claude Code Instructions

Use `AGENTS.md` as the canonical project instructions.

Before editing code:

1. Read `AGENTS.md`.
2. Read `PROGRESS.md`, `DECISIONS.md`, and `feature_list.json`.
3. Work on exactly one task.
4. Record verification evidence before marking anything complete.

